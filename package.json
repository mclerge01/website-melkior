{
  "name": "website-melkior",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Bilingual static website for Melkior Clergé",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --dev && wrangler pages dev dist --port 8788",
    "preview": "node build.js",
    "test": "node --test"
  },
  "dependencies": {
    "libphonenumber-js": "^1.13.6",
    "marked": "^17.0.5"
  },
  "devDependencies": {
    "esbuild": "^0.27.7",
    "wrangler": "^4.99.0"
  }
}
