{
  "name": "website-melkior",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Bilingual static website for Melkior Clergé",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && wrangler pages dev dist --port 8788",
    "preview": "node build.js"
  },
  "dependencies": {
    "marked": "^17.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
